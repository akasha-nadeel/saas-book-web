import { ImportBook } from "@/components/shelf/import-book";

export const metadata = {
  title: "Import a manuscript · OpenChapter",
};

export default function ImportBookPage() {
  return <ImportBook />;
}
